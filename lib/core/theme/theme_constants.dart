import 'package:flutter/material.dart';

const kWidgetVerticalPadding = EdgeInsets.symmetric(vertical: 8);
const kWidgetMinVerticalPadding = EdgeInsets.symmetric(vertical: 2);

const kWidgetHorizontalPadding = EdgeInsets.symmetric(horizontal: 16);
const kPageColumnPadding = EdgeInsets.all(16.0);
const kWidgetVerticalGap = SizedBox(
  height: 16,
);
const kWidgetMinVerticalGap = SizedBox(
  height: 4,
);
